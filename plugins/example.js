exports.run = {
  command: ['example'],
  hidden: [''],
  use: 'string',
  category: 'string',
  async: async (m, {
    client,
    isPrefix
  }) => {
    try {
      // Code Here
    } catch (e) {
      // Error Handling
    }
  }
}